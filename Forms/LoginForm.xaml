<Window x:Class="LoginForm"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="350" Width="450"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Text="User Login" Grid.Row="0" FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <TextBlock Text="Username:" Grid.Row="1" Margin="0,0,0,5"/>
        <TextBox Name="UsernameBox" Grid.Row="2" Height="25" Margin="0,0,0,15"/>
        
        <TextBlock Text="Password:" Grid.Row="3" Margin="0,0,0,5"/>
        <PasswordBox Name="PasswordBox" Grid.Row="4" Height="25" Margin="0,0,0,20"/>
        
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Name="LoginButton" Content="Login" Width="80" Height="30" Margin="0,0,10,0"/>
            <Button Name="CancelButton" Content="Cancel" Width="80" Height="30"/>
        </StackPanel>
    </Grid>
</Window>