<Window x:Class="SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Loading..." Height="300" Width="500"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="White" BorderBrush="DarkBlue" BorderThickness="2" CornerRadius="10">
        <Grid Margin="30">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <TextBlock Text="My Application" Grid.Row="1" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,0,0,20"/>
            
            <TextBlock Text="Loading, please wait..." Grid.Row="2" FontSize="14" 
                       HorizontalAlignment="Center" Margin="0,0,0,15"/>
            
            <ProgressBar Grid.Row="3" Height="20" IsIndeterminate="True"/>
        </Grid>
    </Border>
</Window>